'use strict';

var gates = require('./gates');
var Register = require('./QuantumRegister');

// Exports the Gates class and the Register Class
module.exports = {
  gates: gates,
  Register: Register
};